# This is a minimal list, OK to add things per repo
# mac specific
.DS_Store
.ansible
.azure/
.bash_history
# don't check storage creds into GH
.boto
.cache
*.code-workspace
#  may contain gcloud files
.config
.gitconfig
.local
# .netrc contains secrets for service tokens
.netrc
*.plan
.sentinel
# .ssh dir may contain private keys
.ssh
.terrascan
# Terraform dot files
.terraform
.terraformrc
**/.terraform/*
.terraform.d
*.tfstate
*.tfstate.*
.vscode
.idea
.idea

# Terraform plan and crash files
*.tfplan
crash.log

# Local override variable files (sensitive info)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editor-specific (Visual Studio Code, IntelliJ)
.vscode/
.idea/

__azurite_db_*